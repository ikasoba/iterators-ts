{
  "devDependencies": {
    "typescript": "^4.9.5"
  },
  "name": "iterators",
  "version": "0.1.0",
  "description": "util for iterators",
  "main": ".o/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "install": "tsc --build"
  },
  "author": "github.com/ikasoba",
  "license": "MIT"
}
